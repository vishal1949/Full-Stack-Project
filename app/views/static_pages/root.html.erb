<main id="root"></main>

<script>
    window.images = {
        stayBnbLogo: "<%=asset_path("Staybnb-logo.png")%>",
        cabin: "<%=asset_path("cabin")%>"
    }
</script>
<% if logged_in? %>
    <script type="text/javascript">
        console.log("hi");
        window.currentUser = <%= render(
            "api/users/user.json.jbuilder",
            user: current_user
        ).html_safe %>
    </script>
<% end %>